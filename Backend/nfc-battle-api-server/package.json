{
  "name": "nfc-battle-api-server",
  "type": "module",
  "scripts": {
    "wrangler": "node scripts/wrangler-repo.mjs",
    "dev": "node scripts/wrangler-repo.mjs dev",
    "deploy": "node scripts/wrangler-repo.mjs deploy --minify",
    "deploy:staging": "node scripts/wrangler-repo.mjs deploy --env staging --minify",
    "deploy:production": "node scripts/wrangler-repo.mjs deploy --env production --minify",
    "db:migrate:staging": "node scripts/wrangler-repo.mjs d1 migrations apply nfc-battle-api-server-staging --remote --env staging",
    "db:migrate:production": "node scripts/wrangler-repo.mjs d1 migrations apply nfc-battle-api-server --remote --env production",
    "cf-typegen": "node scripts/wrangler-repo.mjs types --env-interface CloudflareBindings",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "hono": "^4.12.23"
  },
  "devDependencies": {
    "@types/node": "^25.9.2",
    "typescript": "^5.9.3",
    "vitest": "^4.1.8",
    "wrangler": "^4.98.0"
  }
}
